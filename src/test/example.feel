mod demo;

let a Float = 0.1 + 0.3;

let mut b Int = 321 - 300;

let c Int = 2 * 2;

let mut d Int = b + (c * 2);

let T Bool = true;

let F Bool = false;

let g() Int = 1 + 2 + 3 + b;

let add(a Int, b Int) Int = a + b;

let double(x Int) Int = add(x, x) + x + 1;

let blockExpr1 Int = {
    let a = 0;
    let b = 0;
    1 + 2
};

let blockExpr2() Int = {
    let a = 0;
    let b = 0;
    1 + 2
};