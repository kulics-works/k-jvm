mod demo;

let a Float = 0.1 + 0.3;

var b Int = 321 - 300;

let c Int = 2 * 2;

var d Int = b + (c * 2);

var e Bool = true;

var f Bool = false;

let g() Int = 1 + 2 + 3 + b;

let add(a Int, b Int) Int = a + b;