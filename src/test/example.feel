mod demo;

let a = 0.1 + 0.3;

let mut b = 321 - 300;

let c = 2 * 2;

let mut d = b + (c * 2);

let T = true;

let F = false;

let g() = 1 + 2 + 3 + b;

let add(a Int, b Int) = a + b;

let double(x Int) = add(x, x) + x + 1;

let blockExpr1 = {
    let a = 0;
    let b = 0;
    a + b
};

let blockExpr2() = {
    let a = 0;
    let b = 0;
    a + b
};

let blockExpr3 = {};

let compareExpr = {
    let a = 1;
    let b = 1;
    let c = a == b;
    let d = a > b;
    let e = a <> b;
    let f = a < b;
};

let logicExpr = {
    let a = true & true;
    let b = true | false;
};

let ifExpr1 = if(true) 0 else if(true) 0 else 0;

let ifExpr2 = if(true) { 0 } else { 1 };

let ifExpr3 = if(true) {} else {};

let ifStat = {
    if(true) {
        0;
    } else if(true) {
        1;
    } else {
        2;
    };
};

let assignment = {
    let mut a = 0;
    a = 1;
};

let whileStat() = {
    let mut i = 0;
    while (i <= 10) {
        i = i + 1;
    };
};